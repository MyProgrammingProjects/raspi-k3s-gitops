apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: users-microservice-argocd
  namespace: argocd
  labels:
    name: users
    tier: backend
spec:
  project: default

  source:
    repoURL: git@github.com:MyProgrammingProjects/raspi-k3s-helm-charts.git
    targetRevision: main
    path: users-service/helm

  destination:
    server: https://kubernetes.default.svc
    namespace: prod

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true

