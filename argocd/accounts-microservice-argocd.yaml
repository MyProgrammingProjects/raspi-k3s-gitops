apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: accounts-microservice-argocd
  namespace: argocd
  labels:
    name: accounts
    tier: backend
spec:
  project: default

  source:
    repoURL: git@github.com:MyProgrammingProjects/raspi-k3s-helm-charts.git
    targetRevision: main
    path: accounts-service/helm

  destination:
    server: https://kubernetes.default.svc
    namespace: prod

  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      selfHeal: true
      prune: true

